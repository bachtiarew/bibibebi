<!-- partial error message page -->
<% if @skill.errors.any? %>
<div class="panel panel-default">
	<div class="panel-heading">
	Warning !! <%= pluralize(@skill.errors.count, "error") %> happened, read below 
	</div>

	<div class="panel-body">
		<ul>
		<% @skill.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
</div>
<% end %>